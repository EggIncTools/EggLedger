@using Microsoft.AspNetCore.Components.Authorization
@* Router over the shared RCL: its @page components are the routable set. Data pages need an
   authenticated Discord principal (per-user storage); others see the connect prompt. Privacy/
   Terms must stay reachable by anonymous visitors (legal requirement), so they skip the gate. *@
<CascadingAuthenticationState>
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            @if (routeData.PageType == typeof(PrivacyPage) || routeData.PageType == typeof(TermsPage)) {
                <AppErrorBoundary>
                    <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)"/>
                </AppErrorBoundary>
            } else {
                <AuthorizeView>
                    <Authorized>
                        <BackfillTrigger/>
                        <AppErrorBoundary>
                            <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)"/>
                        </AppErrorBoundary>
                    </Authorized>
                    <NotAuthorized>
                        <LayoutView Layout="@typeof(MainLayout)">
                            <ConnectGate/>
                        </LayoutView>
                    </NotAuthorized>
                </AuthorizeView>
            }
        </Found>
        <NotFound>
            <LayoutView Layout="@typeof(MainLayout)">
                <p role="alert">Page not found.</p>
            </LayoutView>
        </NotFound>
    </Router>
</CascadingAuthenticationState>
